{
  "name": "swarmops-web",
  "private": true,
  "license": "Apache-2.0",
  "version": "0.9.0",
  "type": "module",
  "scripts": {
    "predev": "npm run check:vendor",
    "dev": "vite",
    "prebuild": "npm run check:vendor",
    "build": "tsc -b && vite build",
    "lint": "oxlint src",
    "test": "node --experimental-strip-types --test src/*.test.mjs",
    "typecheck": "tsc -b --pretty",
    "check:vendor": "node scripts/check-vendored-kit.mjs"
  },
  "dependencies": {
    "@nim.zone/ui": "file:./vendor/nim-ui",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
